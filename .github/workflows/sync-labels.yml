name: Sync Labels

on:
  workflow_dispatch:

jobs:
  sync:
    uses: vdelrazo/engineering-standards/.github/workflows/sync-labels.yml@main
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
